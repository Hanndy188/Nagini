7* Motor will stop when the distance to the object is 0.5cm.
   motor will rotate when the distance to the object is between 0.5cm and 300cm.
   motor will start when a switch is on.
*/
const int motorDistance = 0.5 cm
const int switchState == ON

void main {
          motorStop( 1, 0, 0.5 );
          motorRun( 1, 100, 300 );
          }
